<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="teacher_attendance.LocationPicker" owl="1">
        <div class="o_location_picker_container w-100 mt-2">
            <div class="d-flex justify-content-between align-items-center mb-2">
                <div>
                    <span class="badge bg-info me-2">Lat: <t t-esc="state.lat.toFixed(6)"/></span>
                    <span class="badge bg-info">Lng: <t t-esc="state.lng.toFixed(6)"/></span>
                </div>
                <button class="btn btn-secondary btn-sm" t-on-click="getCurrentLocation">
                    <i class="fa fa-crosshairs me-1"/> Mi ubicación
                </button>
            </div>
            <div t-ref="mapContainer" style="height: 400px;" class="border rounded shadow-sm bg-light">
            </div>
            <p class="text-muted small mt-1">
                <i class="fa fa-info-circle me-1"/> Haga clic en el mapa o arrastre el marcador para seleccionar la ubicación exacta del aula.
            </p>
        </div>
    </t>
</templates>